import 'package:flame/components.dart';

class Foundation extends PositionComponent {
  // turn on debug mode for this class (temporary before render logic)
  @override
  bool get debugMode => true;
}